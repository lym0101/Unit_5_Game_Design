void game() {
  
}

void gameClicks() {
  //if (dist(mouseX, mouseY, x, y) < d) {
  //  score = score + 1;
  //  success.rewind();
  //  success.play();
  //} else if (dist(mouseX,mouseY,700,100) < 50) {
  //  mode = PAUSE;
  //}else {
  //  lives = lives - 1;
  //  if (lives == 0) {
  //    mode = GAMEOVER;
  //    lives = 3;
  //  }
  //  failure.rewind();
  //  failure.play();
  //}
}
